let Monad =
      https://raw.githubusercontent.com/FormationAI/dhall-bhat/master/Monad/Type

let Free = ./Type

let monad
    : ∀(f : Type → Type) → Monad (Free f)
    = λ(f : Type → Type) → ./applicative f ∧ { bind = ./bind f }

in  monad
